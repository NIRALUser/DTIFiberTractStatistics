# decide what to compile

CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
IF(COMMAND CMAKE_POLICY)
  CMAKE_POLICY(SET CMP0003 NEW)
  CMAKE_POLICY(SET CMP0012 NEW)
ENDIF(COMMAND CMAKE_POLICY)
MARK_AS_ADVANCED(CMAKE_BACKWARDS_COMPATIBILITY)

OPTION(COMPILE_DTITRACTSTAT "Compile dtitractstat." OFF)
IF(COMPILE_DTITRACTSTAT)
  SUBDIRS(dtitractstat)
ENDIF(COMPILE_DTITRACTSTAT)

OPTION(COMPILE_DTIATLASFIBERANALYZER "Compile DTIAtlasFiberAnalyzer." OFF)
IF(COMPILE_DTIATLASFIBERANALYZER)
  SUBDIRS(DTIAtlasFiberAnalyzer)
ENDIF(COMPILE_DTIATLASFIBERANALYZER)

IF(BUILD_TESTING)
  IF(COMPILE_Applications)
  	IF(USE_ITK_LIB)
		IF(USE_QT_LIB)
			SUBDIRS(DTIAtlasFiberAnalyzer)
		ENDIF(USE_QT_LIB)
  	 
  	 	SUBDIRS(dtitractstat)
  	 	
  	ENDIF(USE_ITK_LIB)
  ENDIF(COMPILE_Applications)
ENDIF(BUILD_TESTING)